/*
========================================================================================
   Docker Configuration
========================================================================================
*/

docker {
    enabled = true
    userEmulation = true
    runOptions = '-u $(id -u):$(id -g)'
    fixOwnership = true
}

process {
    // Container settings for each process are defined in the module files
    // This configuration just enables Docker globally
}
